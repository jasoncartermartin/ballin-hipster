<div class="row">
  <div class="col-md-12">
    <H1><%= @wiki.title %></H1>
    <%= @markdown.render(@wiki.body).html_safe %>
    <br/>
    <br />
    <% if current_user %>
      <%= link_to 'Edit', edit_wiki_path(@wiki) %> |
    <% end %>
    <%= link_to 'Back', wikis_path %>
  </div>
</div>